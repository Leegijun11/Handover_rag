# 6. 통합 체크포인트 일정

> 대회 일정 기준: **과제 제출 마감 9/20**, 예선 통과 시 데모데이 10/17
> 오늘(9/4) 기준 문서(0~6번) 확정이 끝났다고 가정한 일정입니다. 실제 진행 속도에 맞춰 조정하세요.

---

## 6-1. 전체 일정표

| 날짜 | 단계 | 내용 | 산출물/확인 사항 |
|---|---|---|---|
| 9/4 (오늘) | 문서 확정 | 0~6번 스펙 문서 전체 확정, 레포 생성 | `guidelines/` 폴더에 문서 업로드, `CLAUDE.md` 작성 |
| 9/5 ~ 9/6 | 스켈레톤 구축 | 폴더 구조 생성, `main.py` 뼈대, `schemas/` 파일 채우기, `.env.example` 작성, Railway/Vercel 레포 연결 | 빈 라우터라도 서버가 켜지는 상태, 배포 플랫폼에서 "Hello World" 수준이라도 1회 배포 성공 확인 |
| 9/7 ~ 9/14 | 1차 병렬 개발 | 각자 담당 모듈 구현 (4번 브리프 기준) | 담당별 진행 상황 공유 (아래 6-2 참고) |
| 9/15 | 중간 체크포인트 | 각자 mock 데이터로 자기 모듈 단독 동작 확인 | 이 시점까지 API 형식(3번)이 실제 구현과 어긋난 부분 없는지 상호 확인 |
| 9/16 ~ 9/17 | 통합 테스트 | 모듈 간 실제 연동 — 전체 시나리오 1회 완주 (아래 6-3 참고) | 사용자등록→문서업로드→배정→체크리스트→챗봇질문→완료체크→리포트생성까지 한 번에 통과 |
| 9/18 | 배포 확정 | Railway Volume(영구 디스크) 연결 확인, 실제 배포 링크로 전체 흐름 재검증 | 로컬이 아닌 배포된 링크에서 6-3 시나리오 재확인 |
| 9/19 | 버퍼 / 버그 수정 | 예비일 — 밀린 작업, 발견된 버그 정리, 제출 자료(README, 발표자료 등) 정리 | - |
| 9/20 | **과제 제출 마감** | 배포 링크 포함 최종 제출 | - |
| (예선 결과 대기) | | | |
| ~10/17 | 데모데이 준비 (예선 통과 시) | 발표 자료, 시연 리허설 | - |

---

## 6-2. 1차 병렬 개발 기간(9/7~9/14) 동안의 소통 방식

- 이 시점엔 서로 다른 모듈이 겹치지 않게 병렬로 짜는 게 목표이므로, 매일 깊게 맞춰볼 필요는 없습니다.
- 다만 **스키마·API를 변경해야 할 필요를 느끼면 그날 바로 공유**하세요 (0번 문서 원칙: 변경 권한은 조장에게만 있음). 발견 즉시 공유하지 않고 각자 임의로 진행하면, 9/15 체크포인트에서 한꺼번에 충돌이 드러나 되돌리는 비용이 커집니다.
- 하루 끝에 "오늘 어디까지 했는지, 막힌 게 있는지"만 짧게 공유하는 정도로 충분합니다.

---

## 6-3. 통합 테스트 시나리오 (9/16~9/17에 반드시 통과할 흐름)

1. 사수 계정으로 `User` 등록 후 로그인 → `access_token` 발급 확인
2. 신입 계정으로 `User` 등록 후 로그인 → `access_token` 발급 확인
3. 사수가 인수인계서 업로드 → `DocumentChapter`/`DocumentChunk` 생성 확인
4. 사수가 해당 신입에게 문서 배정 (`Assignment` 생성)
5. 사수가 체크리스트 작성 — 직접 작성 1건 + AI 초안 생성 후 승인 1건
6. 신입이 챗봇에 질문 2건 이상 (문서에 있는 내용 1건, 없는 내용 1건 — 두 경우 모두 정상 동작하는지)
7. 신입이 체크리스트 항목 1건 이상 완료 체크
8. 사수가 리포트 생성 요청 → 4개 신호가 모두 빈 값 없이 채워지는지 확인
9. 사수가 리포트 히스토리 조회 → 방금 생성한 리포트가 목록에 있는지 확인

이 9단계가 배포된 링크에서 한 번에 끊기지 않고 통과하면, 데모 시연 준비는 끝난 것으로 봅니다.

---

## 6-4. 일정이 밀릴 경우 우선순위

시간이 부족해지면 아래 순서로 범위를 줄입니다 (원래 기획안 7번 "기능 우선순위"와 동일한 원칙).

| 우선순위 | 유지 |
|---|---|
| 필수 | 사용자/배정(로그인·JWT 포함), 문서 업로드(파일+직접 입력), 챗봇(답변+출처), 체크리스트(직접 작성만), 리포트 4개 신호 중 최소 2개(성장곡선, 영역별 히트맵), 5-9번 비용 방어(rate limit + OpenAI 하드 리밋), 데모 체험 모드(회사 3곳 시딩, 1-8/6-6) |
| 여유 시 | 체크리스트 AI 초안 생성, 리포트 나머지 2개 신호(완료-이해 불일치, 침묵 위험), 리포트 히스토리 |
| 제외 가능 | 정식 배포(로컬 시연으로 대체), Git 브랜치 전략의 세부 규칙 |

---

## 6-5. 2차 확장 항목 (1차 빌드 범위 밖)

1차 빌드(9/20 제출)는 이메일 인증 없이 이메일+비밀번호+JWT까지만 구현합니다(1번 문서 §1-1, 3번 문서 §3-9). 아래는 1차에는 넣지 않고, **예선 통과가 확정된 뒤** 필요 여부를 다시 판단하는 항목입니다 — 지금 임의로 구현하지 마세요.

| 항목 | 내용 |
|---|---|
| 이메일 인증 | 회원가입 시 실제 인증 메일 발송·클릭 확인 절차 추가 |
| 비밀번호 재설정 | 이메일 인증이 선행되어야 의미가 있어 함께 묶임 |
| Refresh token | 지금은 access token 단일 발급 + 만료 시 재로그인. 세션 유지가 중요해지면 도입 검토 |

이 표에 항목을 추가하거나 착수 시점을 앞당길 필요가 생기면(예선 결과와 무관하게), 조장에게 먼저 공유 후 이 문서를 갱신합니다.

---

## 6-6. 데모 시드 데이터 준비 (배포 전 1회, 1-8 참고)

배포된 링크에서 "데모로 체험하기"가 바로 동작하려면, 심사 전에 회사 3곳 분량의 데모 데이터를 미리 만들어둬야 합니다.

- `backend/scripts/seed_demo.py` (신규) — 회사 3곳 각각에 대해 사수/신입 `User`, 인수인계서(`Document`/`DocumentChapter`/`DocumentChunk`), `Assignment`, 체크리스트(완료/미완료 섞어서 5개 내외)를 생성하는 1회성 스크립트
- 이미 구현된 각자의 등록/로그인/업로드/배정/체크리스트 로직을 그대로 재사용해서 만든다 (별도 SQL을 새로 짜지 않음)
- 실행 시점: 9/16~9/17 통합 테스트 통과 후, 9/18 배포 확정 전에 1회 실행 (Railway MySQL/ChromaDB에 반영)
- 담당: 조장이 조율 — User·Document·Assignment·Checklist 전 모듈에 걸쳐 있어서, 각자 자기 담당 모델 생성 부분만 스크립트에 채워 넣는 방식으로 진행
- 데모 계정의 이메일/비밀번호는 프론트엔드 상수로 하드코딩 (실 사용자 계정이 아니라 공개해도 무방한 값)
