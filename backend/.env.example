OPENAI_API_KEY=
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=1234
MYSQL_DATABASE=handover_rag
CHROMA_PERSIST_DIR=./chroma_data
JWT_SECRET_KEY=
JWT_EXPIRE_MINUTES=1440
# Fixed (not randomly generated) demo account user_ids — both the 3 demo newcomers AND
# the 3 demo mentors (B-4 added mentor-side demo login too, and /report/generate is
# also OpenAI-backed and mentor-shared). scripts/seed_demo.py must insert these exact
# 6 values as the demo User.user_id rows, not auto-generated UUIDs, so this file and
# the seed script never drift out of sync (guidelines 5-9 item 6, 6-6)
DEMO_USER_IDS=demo-newcomer-a,demo-newcomer-b,demo-newcomer-c,demo-mentor-a,demo-mentor-b,demo-mentor-c
